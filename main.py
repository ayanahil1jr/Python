import math #imports math functions
import time #imports time functions


countdown = 5 #initialize int variables, add decimal to make it a float
name = "Ayan" #initialize String variables
amIHuman = True #initialize boolean variables

#def function_name():
    #defines new functions

#if amIHuman == True:
    #if statements

#while amIHuman == True:
    #while loops
#print(len(name)) #prints the length of a string
#print(name.find("a")) #finds the first character (case-sensitive)
#print(name.capitalize()) #capitalizes the first letter
#print(name.upper()) #capitilizes the entire string
#print(name.lower()) #makes the entire string lowercase
#print(name.isdigit()) #returns true or false if the string is a digit
#print(name.isalpha()) #is the String alphabetical letters (space doesn't count)
#print(name.count("a")) #finds the amount of a specific character in a string
#print(name.replace("a", "o")) #replaces every single parameter string stated to the new parameter string
#print(name*3) #multiplies the string multiple times

#print(int(string, float)) prints something as an integer. also works for str and float
#y = 3.0      y = int(y)   converts something into an integer, works for str and float

#variable = input("statement/question to give to the user")  allows the user to write in an input and assigns in to the variable
#int(input("input")) sets the data type to integer, float, or #string
#pi = 3.14   print(round(pi)) #rounds a number to nearest whole
#print(math.ceil) rounds up to nearest number
#print(math.floor(pi)) rounds down to nearest number
#print(abs(pi)) gives the absolute value of the number, how close it is to 0
#print(pow(pi,2)) raises the first parameter to the power of to the second parameter
#print(math.sqrt(pi)) finds the square root of a number
#x = 1, y = 2, z = 3.   print(max(x,y,x)) finds the largest value between three values
#print(min(x,y,z)) finds the smallest value of each function

#first_name = name[int start] returns a character based on its position in a string
#first_name = name[start:stop] returns characters based on its position from start to excluding the stop number
#first_name = name[start:stop:step] skips characters based on the skip amount. 1 = every character. 2 = every 2nd character. 3 = every 3rd character
#reversed_name = name[::-1] reverses the string
#website= "http://google.com"

#slice = slice(7, -4) #same as indexing[]
#negative indexing, characters get removed from the back first. -4 would remove .com and 7 would remove http://
#print(website[slice]) #prints the website with slice